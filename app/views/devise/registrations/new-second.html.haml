.user-header
  = link_to "#", class: "user-header__img" do
    = image_tag "//www-mercari-jp.akamaized.net/assets/img/common/common/logo.svg?465897195", class: "header__img"
  %nav.progress
    %ul.progress-bar
      %li.progress-bar__text
        会員情報
        .progress-status.through
        .progress-status__bar.through
      %li.progress-bar__text.active
        電話番号認証
        .progress-status.through
        .progress-status__bar
      %li.progress-bar__text
        お届け先住所入力
        .progress-status
        .progress-status__bar
      %li.progress-bar__text
        支払い方法
        .progress-status
        .progress-status__bar
      %li.progress-bar__text
        完了
        .progress-status
.main
  .user-container
    .registration-head
      %h2.registration-head__title 電話番号の確認
    .registration-form
      .form-wrapper
        = form_with model: @user, url: "/user", id:"new_user", class:"new_user", local: true do |form|
          .form-group
            = form.label :phone_number, "携帯の電話番号", class: "label"
            = form.text_field :phone_number, autofocus: true, autocomplete: "phone_number", placeholder: "携帯電話の番号を入力", class: "form-default"
          .form-group
            %p 本人確認のため、携帯電話のSMS(ショートメッセ
            %p ージサービス)を利用して認証を行います。
          %button.registration-form__content__btn
            = link_to "SMSを送信する", "#"
          .form-text-info
            %p ※ 電話番号は本人確認や不正利用防止のために利用
            %p します。他のユーザーに公開されることはありま
            %p せん。
          = link_to "#", class:"explanation-info" do
            電話番号の確認が必要な理由
            = fa_icon "chevron-right", class: "icon-chevron-right"
= render 'footer'