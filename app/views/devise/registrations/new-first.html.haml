.header
  = link_to "#", class: "header__link" do
    = image_tag "//www-mercari-jp.akamaized.net/assets/img/common/common/logo.svg?465897195", class: "header__img"
  %nav.progress
    %ul.progress-bar
      %li.progress-bar__text.active
        会員情報
        .progress-status.through
        .progress-status__bar
      %li.progress-bar__text
        電話番号認証
        .progress-status
        .progress-status__bar
      %li.progress-bar__text
        お届け先住所入力
        .progress-status
        .progress-status__bar
      %li.progress-bar__text
        支払い方法
        .progress-status
        .progress-status__bar
      %li.progress-bar__text
        完了
        .progress-status
.main
  .container
    .registration-head
      %h2.registration-head__title 会員情報入力
    .registration-form
      .form-wrapper
        = form_with model: @user, url: "/user", id:"new_user", class:"new_user", local: true do |form|
          .form-group
            = form.label :nickname, "ニックネーム", class: "label"
            %span.essential 必須
            = form.text_field :nickname, autofocus: true, autocomplete: "nickname", placeholder: "例)メルカリ太郎", class: "form-default"
          .form-group
            = form.label :email, "メールアドレス", class: "label"
            %span.essential 必須
            = form.email_field :email, autofocus: true, autocomplete: "email", placeholder: "PC・携帯どちらでも可", class: "form-default"
          .form-group
            = form.label :password, "パスワード", class: "label"
            %span.essential 必須
            = form.password_field :password, autofocus: true, autocomplete: "password", placeholder: "7文字以上", class: "form-default"
          .form-group
            = form.label :password, "パスワード（確認）", class: "label"
            %span.essential 必須
            = form.password_field :password, autofocus: true, autocomplete: "password", placeholder: "7文字以上", class: "form-default"
          .form-group
            %h3 本人確認
            %p 安心・安全にご利用いただくために、お客さまの本人
            %p 情報の登録にご協力ください。他のお客さまに公開さ
            %p れることはありません。
          .form-group
            = form.label :first_name, "お名前（全角）", class: "label"
            %span.essential 必須 
            .form-flex
              = form.text_field :first_name, autofocus: true, autocomplete: "first_name", placeholder: "例）山田", class: "form-half"
              = form.text_field :last_name, autofocus: true, autocomplete: "last_name", placeholder: "例）彩", class: "form-half"
          .form-group
            = form.label :first_name_kana, "お名前カナ（全角）", class: "label"
            %span.essential 必須
            .form-flex
              = form.text_field :first_name_kana, autofocus: true, autocomplete: "first_name_kana", placeholder: "例）ヤマダ", class: "form-half"
              = form.text_field :last_name_kana, autofocus: true, autocomplete: "last_name_kana", placeholder: "例）アヤ", class: "form-half"
          .form-group
            = form.label :birth_year_on, "生年月日", class: "label"
            %span.essential 必須
            .form-flex
              = form.email_field :birth_year_on, autofocus: true, autocomplete: "birth_year_on", class: "select-default"
              = fa_icon "chevron-down", class: "icon-chevron-down"
              %span.date-span 年
              = form.email_field :birth_month_on, autofocus: true, autocomplete: "birth_month_on", class: "select-default"
              = fa_icon "chevron-down", class: "icon-chevron-down"
              %span.date-span 月
              = form.email_field :birth_day_on, autofocus: true, autocomplete: "birth_day_on", class: "select-default"
              = fa_icon "chevron-down", class: "icon-chevron-down"
              %span.date-span 日
          .form-text-info
            %p ※ 本人情報は正しく入力してください。会員登録後、
            %p 修正するにはお時間を頂く場合があります。
          .form-group
            私はロボットではありません（recaptcha_tags使用予定）)
          .form-group
            %p「次へ進む」のボタンを押すことにより、利用規約に
            %p 同意したものとみなします
          %button.registration-form__content__next
            = link_to "次へ進む", "#"
          = link_to "#", class:"parsonal-info" do
            本人情報の登録について
            = fa_icon "chevron-right", class: "icon-envelope"
= render "footer"
